{
  "name": "fabric",
  "version": "1.0.0",
  "description": "This guide uses Hyperledger Fabric samples `test-network` to run a local network, deploy chaincode `estathub` on channel `mychannel`, and configure the backend to connect via the Fabric Gateway.",
  "main": "enrollAppUser.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fabric-network": "^2.2.20"
  }
}
